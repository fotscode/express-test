extends ../layout.pug

block content
  form(action="/lead/"+lead.id+"/edit", method="post")
    input(name="lead_email", type="email", placeholder="Enter an email", value=lead.email)
    button(type="submit") Save
